theory
  zero	: nat.
  s	: nat -> nat.
  +	: (nat, nat) -> nat.
  id	: nat -> nat.
  inductive s.
  inductive zero.
  forall y: nat. (zero() + y) = y.
  forall x: nat, y: nat. (s(x) + y) = s((x + y)).
  forall x: nat. id(x) = x.
conjecture
  forall x: nat, y: nat. (id(x) + y) = (y + x).

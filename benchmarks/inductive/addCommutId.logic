theory
  zero	: nat.
  s	: nat ↦ nat.
  +	: (nat, nat) ↦ nat.
  id	: nat ↦ nat.
  inductive s.
  inductive zero.
  ∀ y: nat. (zero() + y) = y.
  ∀ x: nat, y: nat. (s(x) + y) = s((x + y)).
  ∀ x: nat. id(x) = x.
conjecture
  ∀ x: nat, y: nat. (id(x) + y) = (y + x).

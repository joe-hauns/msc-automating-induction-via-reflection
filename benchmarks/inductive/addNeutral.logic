theory
  zero	: nat.
  s	: nat ↦ nat.
  +	: (nat, nat) ↦ nat.
  inductive s.
  inductive zero.
  ∀ y: nat. (zero() + y) = y.
  ∀ x: nat, y: nat. (s(x) + y) = s((x + y)).
conjecture
  ∀ x: nat. (x + zero()) = x.
